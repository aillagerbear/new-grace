import React from 'react';

/**
 * SidebarLeft component for the zod.kr clone.
 * Contains login CTA, utility menu (Sticker, Quest, Attendance), and vertical banners.
 */
export default function SidebarLeft() {
  return (
    <aside id="app-sidebar-left" className="w-[250px] shrink-0 hidden lg:block">
      <div className="flex flex-col gap-6"></div>
    </aside>
  );
}